# Python Test

A simple Python project created to verify and practice a development environment setup.

## Environment

- Python 3.14.7
- Virtual environment (`venv`)
- pip
- Visual Studio Code
- Git
- GitHub

## Dependencies

Install the project dependencies with:

```bash
python -m pip install -r requirements.txt
```

## Run

Run the project with:

```bash
python main.py
```